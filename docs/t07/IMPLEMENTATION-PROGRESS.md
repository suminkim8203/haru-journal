# T07 구현 진행 — 2026-09-22

요약: 2026-09-22 인증 기반 앱을 운영 전환했고, 2026-09-23 계정 화면을 별도 주소로 정리했다. 아래 초기 미적용 문장은 당시 단계 기록이다. 최신 검증 상태는 문서 끝의 후속 항목을 따른다.

## 로컬 구현 및 검증

- 새 비밀번호 검증: 8자 이상, 공백 금지, 한글 허용, 조합 강제 없음. 입력 자동 변환 없음. UI 연결 및 서버 Auth 강제 조건 검증은 미완료.
- 계정-다이어리 1:1 연결 테이블과 서버 세션 검사. 이메일 미확인, 세션 삭제, 7일 초과, 다른 사용자의 세션 거절.
- 복구/OTP 중간 세션은 자료 접근 거절. 현재 기반은 password AMR만 허용하므로 가입 확인 직후 연결 흐름은 추가 구현 대상.
- 기존 SQL 0001–0010을 모두 설치한 로컬 PostgreSQL 호환 환경에서 공개 고정 ID를 계정별 ID로 바꾸는 전환 SQL을 실행하고 목록/내보내기 분리, 양방향 변경·삭제·타인 계획에 생성 거절, 거절 후 자료 불변을 검증.
- 새 테스트 6개 통과. Auth 테이블과 JWT 주입은 로컬 검증용 대역이다. 실제 Auth 로그인·HTTP 403/404·발송·로그아웃 증거로 제출하지 않는다.

## 운영 적용 전 남은 작업

staged-t07 디렉터리는 자동 적용 마이그레이션이 아니다. 공개 RPC 차단은 메일, 가입 화면, 서버 제한, 기존 자료 소유자 확인과 함께 전환해야 한다. 실제 NAS에는 적용하지 않았다.

개별 조회 404 증거, 가입/복구 번호 시도 제한, 비밀번호 변경 후 전 세션 종료, 서버 비밀번호 조건, 사용자 생성/자료 이전, 계정 삭제/백업 정책, 내보내기 화면, 실제 5일 사용은 미완료다. 로컬 테스트가 이를 대신하지 않는다.

## 메일 연결

Resend auth.haruleaf.com 도메인 검증 완료를 관리 화면에서 확인. 발송 지역 Tokyo. Cloudflare rsend.auth CNAME을 Resend 요구값 rsend-apne1.forge.rmta.net으로 보정했다. 웹 서비스 A 레코드와 기존 라우팅은 변경하지 않았다.

발송 전용·auth.haruleaf.com 제한 API 키 생성 양식 준비. 신규 발송 권한 생성 및 NAS 저장 확인 대기. 키 미생성, SMTP 미연결, 실제 메일 수신 미검증.

## 메일 설정 후속

전체 로컬 테스트 61/61 통과. 사용자 승인으로 도메인 제한 발송 키를 생성해 NAS Supabase SMTP 설정에 반영, SMTP 연결 인증 성공 및 기존 Auth 이미지 보존 확인. 실제 메일 미발송.

키 표시 대화상자를 닫는 직후 브라우저 도구가 닫히는 중인 내용을 출력하여 비밀값이 노출됨. 원문은 이 문서에 기록하지 않는다. 해당 키로 발송을 진행하지 않고 폐기·재발급 승인을 요청했다. 교체 및 검증 완료 전 SMTP 준비 완료로 판정하지 않는다.

## 메일 연결 검증 완료

노출 키 폐기 확인 후 동일한 발송 전용·도메인 제한 키를 재발급하여 NAS 설정 교체 완료. 교체 키는 출력하지 않았다. SMTP 연결 확인 메일 한 통을 발송하고 사용자에게 실제 도착 확인을 받았다. 가입 OTP·복구 OTP 수신 검증은 아직 별개로 남아 있다.

가입 확인/비밀번호 재설정용 HTML 메일 템플릿 소스 추가. 외부 이미지·추적·웹폰트 없이 제호/지면/큰 번호 구성. 이 템플릿은 아직 서버에 연결하지 않았으며 10분 문구는 실제 Auth 설정과 함께 적용해야 한다. 메일 클라이언트별 시각 검증 미완료.

## 인증 연결 화면 구현

로컬 /auth에 실제 SDK signup / signup OTP verify / password login / recovery 요청 / recovery OTP verify / password update / signOut 호출을 연결했다. 인증 검증용 경로이며 개인 다이어리를 표시하지 않는다. 가입 OTP 완료 후 일반 비밀번호 로그인으로 안내하여 복구/OTP 세션을 자료 접근 권한으로 취급하지 않는다. 토큰은 현재 메모리에만 유지한다. 새 화면은 공개 배포 전이다.

타입 검사 통과. 로컬 브라우저에서 로그인 화면 렌더링을 확인하고 주요 제목을 승인한 조선일보명조 역할로 보정했다. 실제 가입과 복구 호출 검증은 아직 수행하지 않았다. Auth v2.196.0 소스상 최소/최대 길이는 바이트 기준이며 최대 72바이트이다. UI는 승인된 글자 수·공백 정책을 검사하지만 서버 기본 설정과 동일하지 않아 추가 보호가 필요하다.

메일 템플릿·OTP·비밀번호·JWT 설정과 내부 템플릿 컨테이너 적용은 자동 승인 검토가 운영 변경 범위 승인을 요구하여 실행되지 않았다. 사용자에게 범위를 명시해 승인 요청 중이다.

## 승인 범위 NAS 적용 완료

사용자의 명시적 범위 승인 후 2026-09-22 적용. 내부 haru-auth-mail 컨테이너는 기존 웹과 동일한 nginx 이미지 digest 사용, 외부 호스트 포트 없음. Supabase Auth에서 confirmation/recovery HTML 템플릿 내부 fetch 성공 및 설정값 일치 확인. OTP 6자리/600초, SMTP 재발송 60초, 최소 길이8(서버는 바이트 기준), JWT3600초. 기존 Auth 이미지 보존 및 /auth/v1/health v2.196.0 정상 응답. 공개 다이어리·DB·앱 배포는 미변경.

번호당 오입력5회, 한글 글자 수·공백 서버 검사, 복구 전용 권한 전체 경로 검증은 여전히 미완료. 사용자에게 로컬 /auth에서 직접 비밀번호 설정·가입 이메일 확인·로그인을 요청했다. 실제 가입·복구 성공을 아직 확인하지 않았으므로 구현 완료로 표시하지 않는다.


## 사용자 가입·로그인 확인 및 메일 제목 수정

사용자가 실제 메일의 제목을 확인했고 로그인이 성공했다고 알렸다. 사용자 확인 증거이며 독립 HTTP 성공/실패 증거는 별도 수집 대상이다. 메일 한글 제목의 Georgia/Batang fallback을 시스템 고딕(Apple SD Gothic Neo/Malgun Gothic/Arial/sans-serif),24px,600으로 수정한다. 제호는 유지. 가입 확인과 비밀번호 재설정에 동일 적용. 메일 클라이언트의 실제 대체 글꼴은 기기에 따라 달라진다.

메일 제목 수정 NAS 반영 완료: confirmation/recovery 양쪽 내부 응답과 소스 SHA256 일치 확인. Auth 정상 응답 확인. 기존 받은 메일은 변경되지 않고 후속 발송에 적용됨. 실제 수정 메일의 수신 화면은 사용자 확인 전.



## 재설정 오류 구분 수정

사용자가 기존 비밀번호와 같은 값을 입력했을 때 모든 updateUser 오류를 재인증 안내로 묶던 문제가 확인됐다. same_password는 현재 입력 화면과 세션을 유지하고 다른 새 비밀번호 입력을 안내한다. 조건 오류·요청 제한·일반 실패도 재인증을 강제하지 않는다. 명시적 세션/재인증 오류만 이메일 인증 재시작 버튼을 제공한다. 실제 보고된 요청의 오류 코드 원문은 수집하지 않았으며 코드 분기와 공식 오류 정의에 따라 보정했다.

사용자는 같은 재설정 화면에서 다른 비밀번호 입력 후 성공했다고 확인했다. 당시 인증은 유지되고 있었으며 재인증 안내가 잘못된 것으로 사용자 흐름에서도 확인됨. 새 비밀번호와 확인값은 양쪽 입력 변경 시 즉시 비교하고, 확인란 입력 전에는 불일치 경고를 표시하지 않는다. 불일치/비밀번호 조건 오류일 때 제출 비활성화, 서버 제출 시 재검증도 유지한다.

## 개인 자료 직접 조회·신규 계정 준비 — 2026-09-22

사용자가 비밀번호 재설정 오류 안내 및 실시간 확인 입력을 직접 확인 완료했다.

운영 미적용 staged SQL에 직접 자료 조회 haru_resource를 추가했다. 기존 snapshot 표현을 재사용하며 다른 계정/없는 자료는 모두 PT404로 거절한다. 새 계정 haru_setup_account는 비밀번호 인증·확인된 이메일·유효한 서버 세션을 검증하고 계정 행 잠금 후 빈 다이어리를 한 번만 만든다. 외부 소유자 ID를 입력받지 않으며 기존 T06 자료를 자동 배정하지 않는다. 기존 연결이 있으면 그대로 반환한다.

로컬 인증/오류/개인 RPC 7개 테스트 통과: 양방향 조회·수정·삭제 거절, 목록/내보내기 분리, 세션 삭제 후 같은 인증값 거절, 만료/복구 거절, 신규 빈 다이어리 및 재호출 동일 ID, OTP 중간 인증의 생성 거절. Auth 테이블·JWT는 검사 대역이며 실제 NAS HTTP 증거가 아니다. 실제 동시 요청 검증은 미완료다.

다음: 앱 인증 상태와 저장 클라이언트 연결, 서버 비밀번호 조건·OTP 시도 제한, 기존 본인 자료 이전, NAS에서 실제 요청/응답 증거. 운영 공개 RPC와 자료는 이번 작업으로 바뀌지 않았다.

## 로그인과 개인 다이어리 연결 — 2026-09-22

구현: 인증/저장 SDK를 공유하고 비공개 모드(NEXT_PUBLIC_HARU_PRIVATE=1)에서 서버 haru_setup_account 성공 후에만 JournalApp을 마운트한다. 비공개 모드는 인증 상태 복원·자동 토큰 갱신을 사용한다. 인증값은 SDK 브라우저 저장소에 보관되므로 XSS 위험은 별도로 남는다. 서버 세션 만료/소유권 검사는 필수이며 UI 숨김을 보안 근거로 삼지 않는다. 로그아웃·계정 변경 시 자료 컴포넌트와 개인 다이어리 ID를 비우며 늦게 도착한 계정 준비 응답이 상태를 복구하지 못하도록 세대 검사를 둔다.

조회/내보내기는 서버가 연결한 개인 다이어리 ID와 일치해야 한다. T06 고정 ID는 공개 모드에서만 유지한다. 공개/비공개 플래그는 배포 전환 수단이며 보안 경계가 아니다. DB 공개 RPC 폐쇄 없이 플래그만 켜면 비공개 전환 완료가 아니다.

05_password_revocation.sql은 비밀번호 해시 변경 시각을 비공개 표에 기록하고 그 이전 서버 세션의 자료 요청을 거절한다. 브라우저 global signOut 실패와 별개로 자료 접근을 막기 위한 단계이며 실제 Auth API 전체를 무효화했다고 주장하지 않는다. 원문 비밀번호/해시를 이 표에 복제하지 않는다. 실제 NAS 미적용.

검증: 타입 검사 및 비공개 정적 빌드 통과. 127.0.0.1:3222의 실제 브라우저에서 로그인 폼만 보이고 일정 메뉴/개인 자료가 표시되지 않음을 확인했다. 로그인 후 실제 NAS 저장은 아직 미검증. 전체 검사 64개 중 63개 통과, 루틴 검사 하나는 무작위 ID 순서의 첫 항목이 변경하려는 날짜와 일치하는 테스트 오류였다. 오늘 날짜 항목을 명시적으로 고르게 보정한 뒤 루틴 8개 모두 통과. 신규 개인 snapshot 계약 1개 및 변경된 권한/비밀번호 무효화 검사도 통과.

서버 정책 확인: 설치 Auth v2.196.0 password.go의 len(password)는 바이트 기준이고 RequiredCharacters는 필수 포함 문자 검사다. 승인된 한글 8글자·공백 금지 검증을 그대로 강제하는 설정으로 볼 수 없다. 이메일 발급 건별 5회 오입력 제한 또한 현재 서버에 구현되지 않았다. 추가 서버 검증 경로와 직접 Auth 우회 차단을 함께 구현해야 하며, 클라이언트 카운터만으로 대체하지 않는다.

근거: https://raw.githubusercontent.com/supabase/auth/v2.196.0/internal/api/password.go , https://supabase.com/docs/guides/self-hosting/self-hosted-auth-hooks , https://supabase.com/docs/guides/auth/rate-limits

남은 범위: 서버 입력/OTP 시도 검증, 본인 계정 T06 이전, 실제 양방향 HTTP 증거, 계정 삭제/전체 세션 정책, 운영 전환. 공개 서비스는 이번 작업으로 변경하지 않았다. 사용자 비밀번호/인증번호를 대신 입력하지 않았다.

## 개인 계정 운영 전환·실제 HTTP 검증 — 2026-09-22

요약: 실제 사이트를 로그인 방식으로 전환했고, 사용자가 본인 계정 로그인 뒤 기존 기록이 보인다고 확인했다. 아래 기록이 앞선 미적용 상태보다 최신이다.

- NAS staged SQL 01–05 적용. 기존 16개 자료 테이블의 행 수와 행 내용 서명을 동일 트랜잭션 안에서 비교하고 불일치 시 rollback하도록 했다. 본인 계정에 기존 T06 다이어리를 연결했고 원래 자료는 변경하지 않았다.
- 웹 release: t07-auth-6db8912be180. 전환 전 DB 백업: /opt/supabase/backups/t07-cutover-20260922-055600/database.dump. 이전 웹 release: approved-50c0038302f8.
- 최초 자동 승인 검토는 데이터 불변 검사 실패 후에도 진행할 수 있는 스크립트를 거절했다. 불일치 시 트랜잭션 전체 중단과 웹 전환 중단을 구현하고 rollback 검사를 거친 수정본으로 적용했다.
- 익명 snapshot/export/setup 요청은 401. 로그인 전 브라우저에는 로그인 화면만 표시됨. 사용자 실제 로그인과 기존 기록 표시는 사용자 확인 완료.
- 검증 계정 A/B의 본인 조회 200, A→B와 B→A 읽기·수정·삭제 총 6건 404. 각 목록은 본인 계획 1건만 포함하며 상대 자료는 거절 전후 동일하다.
- 같은 POST /rest/v1/rpc/haru_snapshot, 같은 인증값으로 로그아웃 전 200 / 후 401. 실제 서버 요청 증거는 evidence/private-http-2026-09-22.json. 인증값·비밀번호는 기록하지 않았다.
- 서버 Auth v2.196.0 / Edge Runtime v1.76.2. 06_auth_attempts.sql과 auth-guard 설치 및 외부 Auth 요청 경로 연결. Envoy 후보 설정 검증 통과 후 적용, 실패 시 기존 설정 복구 절차 포함.
- 한글 3글자 비밀번호 가입 요청은 422. 같은 검증 대상의 6자리 번호 검증은 첫 5회 403, 6회째 429. 이메일·용도별 해시 키에 10분 시도 창을 적용한다. 재발송으로 실패 횟수를 초기화하지 않는다. 이는 정확히 발급 번호별 카운터가 아니라 이메일·용도별 더 엄격한 제한이다.
- 전체 로컬 검사 67/67 통과. 이는 실제 HTTP 검증과 별개다.

남은 확인: 비밀번호 변경 후 예전 인증값 재사용 실제 HTTP 증거, 비밀번호 저장 해시 비교, URL/헤더/본문의 소유자 위조, 내보내기 사용자 흐름, 로그인 중 비밀번호 변경 UI, 계정 삭제와 백업 정책, 실제 5일 기록. T07 완료 또는 제출 가능 상태로 판정하지 않는다.

한계: 브라우저 저장소의 인증값은 XSS 위험이 있다. 서버의 세션 존재·7일 상한·비밀번호 변경 시각 검사는 다이어리 RPC 접근에 적용된다. Auth 모든 API의 동일한 7일 정책을 검증했다고 설명하지 않는다. 인증 프록시를 거친 요청의 실제 클라이언트 IP별 제한은 별도 확인이 필요하다.

## 7일 탈퇴 유예 적용
사용자 승인으로 신청·취소·기한 후 자동 삭제를 구현하고 배포했다. 기존 자료 불변·예약 작업 정상·68개 검사 통과. 범위와 백업 복구 한계는 ACCOUNT-DELETION.md를 따른다. 실제 7일 경과 관찰 및 사용자 화면 확인과 구분한다.

## 2026-09-23 최신 상태 — 백업 정리 자동화 및 안내 배포

요약: 관리 중인 백업의 계정별 정리 작업을 NAS 예약 작업에 연결했고, 서비스 안내도 배포했다. 실제 168시간 경과 관찰과 모든 외부 사본 검증을 완료한 것은 아니다.

- 백업 작업 설치, timer active, 최초 실행 success. 설치 당시 만료 계정 0건으로 운영 계정 삭제나 원본 백업 교체는 없었다.
- 격리 복구 사본에서 대상 계정 제거 후 다른 journal 기록 불변 및 재복원 일치를 확인했다. 전체 작업 시험은 폐기 가능한 사본에서 만료 시각과 운영 계정 조회를 대역 처리했다. 6개 파일 처리, 반복 실행 변경 0건. 원본 백업과 운영 자료는 변경하지 않았다.
- 관리 범위: /backup/haru-operations, /opt/supabase/backups, /backup/haru-archive-preview 및 지정된 격리 시험 사본. 알려진 DB dump·journal SQL·다이어리 snapshot을 재생성한다. 알려진 중지 상태 복구 컨테이너도 정리한다.
- 백업과 삭제는 같은 잠금으로 직렬 실행한다. 대상 외 journal 행이 달라지거나 복원이 실패하면 완료로 기록하지 않는다. 외부 삭제 이력은 복구 후 재적용을 위해 별도로 유지한다.
- 웹 release t07-backup-dc86e5cb8684 적용. 공개 HTML/JS/CSS 등 19개 파일이 빌드와 일치한다. DB 변경 없음. 화면에는 추가 백업 유예기간 없음과 장애 시 지연 가능성을 표시한다.
- 이전 대조 오류는 정렬 규칙과 시간대 차이였다. C 정렬·UTC 기준 새 백업의 19개 journal 테이블 일치를 검증했다. 로컬 69개 검사, 타입 검사, 비공개 정적 빌드 통과.

한계: Proxmox 스냅샷·외부 장비 사본의 존재와 삭제 연동은 현재 확인되지 않음. 새로운 이름/형식의 백업을 자동으로 모두 식별한다는 보장은 없다. 운영 감사 로그나 물리 디스크 잔존 영역의 완전 삭제를 검증한 것이 아니다. 임의의 수동 복구는 서비스 재개 전 외부 삭제 이력을 재적용해야 한다. 실제 7일 관찰은 아직이며 T07 전체 완료로 판정하지 않는다.

근거: evidence/backup-erasure-deployment.json, evidence/backup-erasure-drill-report.json, evidence/backup-policy-web-report.json, evidence/backup-policy-http.json.

## 로그인 중 비밀번호 변경 추가 — 2026-09-23
구현·배포·19개 파일 일치 확인. 실제 비밀번호 입력과 새 비밀번호 재로그인은 사용자에게 확인 요청했다. 이메일 재설정과 구분하며 상세는 PASSWORD-CHANGE.md를 따른다. T07의 5일 실제 기록과 나머지 보안 증거는 아직 미완료다.

## 계정 메뉴 구조 후속 조정 — 2026-09-23
사용자 피드백에 따라 로그인 중 변경 버튼을 현행 UI에서 제거하고 계정·탈퇴를 각각 독립 주소로 분리했다. 로컬 빌드/타입 및 9개 인증 검사 통과. 아래 배포 기록이 최신이며 상세는 ACCOUNT-NAVIGATION.md를 따른다.

## 배포 및 후속 검증 — 2026-09-23

- release `t07-account-9ebfdfb4c271` 배포. 기존 웹 릴리스만 교체했으며 배포 도구는 DB를 변경하지 않았다.
- 공개 HTML/JS/CSS 21개가 검증한 정적 빌드와 해시 일치. `/account.html`과 `/account/deletion.html`은 공개적으로 200을 반환한다.
- 실제 비로그인 브라우저에서 `/account.html`을 열면 계정 자료와 조작 대신 로그인 화면이 나타났다.
- 로그인된 실제 사이트에서 일정 메뉴의 계정 링크, 별도 계정 화면, 별도 탈퇴 화면, 일정 복귀를 브라우저로 확인했다. 탈퇴 신청/삭제는 실제 사용자 계정으로 실행하지 않았다.
- 로그인 중 비밀번호 변경은 현재 화면에서 제거했고, 이메일 코드로 본인을 확인하는 비밀번호 재설정은 유지한다. 이전 인증값 거절은 별도 검증 과제다.

## 실제 5일 사용 기준 준비 — 2026-09-23

사용자가 질문·단일 지표·규칙 변경 방향을 승인했다. 이전 4일의 인증 전 작업은 소급하지 않는다. 본인 계정에 준비 계획·할 일을 저장했고 17:20–17:27 KST Codex 작업 타이머는 7분 7초로 실행 상세·돌아보기에 일치했다. 이는 AI 개발 검증이며 사용자 관찰 1일차로 센 앞선 표기는 정정한다. 사용자 실제 5개 날짜와 2일차 뒤 규칙 변경은 아직 미충족이다. 기준·출처·계산 규칙은 [실제 5일 기록](FIVE-DAY-STUDY.md)을 따른다.

기존 시험 계정 A/B의 같은 임시 비밀번호가 서로 다른 60자 `$2a$10$` 해시로 저장됨을 NAS Auth DB에서 읽기 전용으로 확인했다. 방법·한계는 [시험 계정 해시 확인](PASSWORD-HASH-READONLY.md)을 따른다. 전체 해시 제출 증거, 로그인/서버 로그의 원문 부재 및 비밀번호 재설정 뒤 이전 인증값의 실제 HTTP 거절은 아직 별개다.

## 2026-09-23 — 증거 범위 재점검

- 여섯 항목의 `AUTH-IMPLEMENTATION-DRAFT.md` 작성. 실제 HTTP 증거, 로컬 SQL 검사, 미검증 항목을 분리했다.
- 헤더·본문의 소유자 ID 위조로 상대 자료가 바뀌지 않는 로컬 검사를 강화했고 관련 테스트 5개가 통과했다. 실제 HTTP 위조 검증은 별도 미완료다.
- 추적 파일·도달 가능한 Git 이력·정적 빌드의 특정 비밀 형식을 검사했다. 결과는 `evidence/public-secret-scan.json`; 일반 비밀번호·서버 로그 전체 검사를 대신하지 않는다.
- 고정 T06 commit이 현재 HEAD의 조상임을 `git merge-base --is-ancestor` 종료 코드 0으로 확인했다.
- AI의 427초 검증을 사용자 실제 1일차로 표시한 것을 정정했다. 사용자의 실제 5일 관찰은 미완료다.
- 이번 변경은 테스트·문서·검사 도구이며 운영 앱 재배포를 요구하지 않는다.
