> **과거 기록:** 작성 시점의 설계·검토·검증 결과입니다. 현재 구현과 승인 기준은 [문서 시작점](../../README.md)을 따릅니다.

# T06 02.10 테스트 보고서

한 줄 요약: 실제 HTML 스크립트를 실행한 로직 검사 31개가 통과했습니다. 실제 브라우저 클릭·드래그·모바일 시각 검증은 미완료입니다.

## 수행 방법

2026-09-16. Node의 독립 실행 환경에서 결과 HTML의 실제 스크립트를 실행했습니다. 문서 요소와 저장소는 대체 객체를 사용했습니다. 검사 전용 내부 접근 지점은 메모리에만 추가했으며 결과 HTML에는 넣지 않았습니다.

## 현재 구현에 대한 검사 결과

| 범위 | 검사 | 결과 |
|---|---|---|
| 기존 동작 17개 | 구문/초기화, 멈춤 재분류, 정정 이력, 구간 오류, 자정 분할, 필터, 드롭 초안/취소/확인/실패, 돌아보기 이동, 회고/책갈피, 주·월간 생성, 대체 드래그 이벤트 | 통과 |
| 루틴 계획 | 계획 유형 저장, 반복 입력으로 연결 | 통과 |
| 시각 미정 반복 | 날짜별 독립 항목 생성, 예정 블록 미생성, 일간 날짜 필터와 표시 | 통과 |
| 완료 | 한 날짜 완료 후 다음 날짜 미완료 유지 | 통과 |
| 날짜별 수정 | 다른 날짜 미변경, 개별 예외 표시 | 통과 |
| 명시적 배치 | 하루 배치를 바꾼 항목의 예외 표시 | 통과 |
| 이후 반복 | 완료·개별 수정·배치 수정·실행 기록 항목 보존 | 통과 |
| 겹침/실패 | 겹침 확인 전 차단, 모의 저장 실패 후 초안 유지·재시도 | 통과 |
| 반복 중단 | 예정 배치 제외, 개별 예외 및 실제 기록 보존 | 통과 |
| 집계 | 중단 항목 대상 제외 및 별도 개수 | 통과 |
| 입력 오류 | 계획 밖 기간, 시각 누락, 예상 시간 0 거절 | 통과 |
| 검토 예시 | 고정 시각 7개 배치와 시각 미정 7개 항목 추가 | 통과 |
| 건너뛰기 | 확인 후 한 날짜만 제외, 다음 날짜 유지, 집계 제외 | 통과 |
| 제외 상태 보호 | 건너뛴 항목 완료·재배치 차단 | 통과 |
| 전체 화면 생성 | 대체 문서 환경에서 일정·계획·돌아보기 렌더링 호출 | 통과 |

합계: 31/31. 이 수치는 실제 사용자 조작 테스트 통과 수가 아닙니다.

## 미검증 및 제한

- 브라우저의 로컬 HTML 접근 제한 때문에 실제 포인터 드래그, 클릭, 키보드 초점, 모바일 화면 배치를 검증하지 않았습니다. 제한 우회는 하지 않았습니다.
- 기간 연장/축소, 날짜 선택기, 타이머를 포함하는 모든 조합을 전수 검사하지 않았습니다.
- 최대 366일 시안 제약 및 보호 규칙은 코드에 있으며, 대규모 생성 성능과 실제 서비스 한도는 검증하지 않았습니다.
- 실제 브라우저 새로고침 복원·여러 탭 충돌은 사용자 환경 검사가 필요합니다.
- 보존 대상 02.7/02.8/02.9의 해시를 확인했습니다. 기준 파일은 변경하지 않았습니다.

## 재현

검토 ZIP에 포함한 `test-v210.cjs`는 작업 폴더의 `work/T06-flow-v2.10.html`을 읽습니다. HTML을 해당 경로에 두고 `node test-v210.cjs`로 로직 검사를 재현할 수 있습니다. 이 검사를 브라우저 검증으로 해석하지 않습니다.
