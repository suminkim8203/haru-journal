# 프로젝트 작업 지침

요약: 과제 기준, 최신 사용자 결정, 실제 코드 상태를 대조한 뒤 작업한다.

## 필수 RULE

- 작업 시작 시 [T06 과제 RULE](docs/T06-ASSIGNMENT-RULE.md)을 읽고 적용한다.
- 이 RULE은 과제 원문을 바탕으로 하되 사용자가 지정한 ‘본인 실제 자료만 사용해야 한다는 조건 제외, 예시·더미 허용’ 예외를 반영한 작업 기준이다. 원문 보관본을 변경하거나 외부 평가 조건도 변경됐다고 설명하지 않는다.
- 서버 실제 DB 저장/복원, 자료 개수/집계/근거, 막힌 이유/막힘 수/다음 계획 개선점 전달 등 나머지 필수 기준은 유지한다.
- 과제 RULE과 최신 UI/기능 결정이 충돌하면 구현 전에 차이를 알리고 해결안을 정리한다. 이미 승인된 결정을 반복 질문하지 않는다. 합의한 가역적 작업은 계속 진행한다.
- 과제 지시문에서 벗어나는 기능 제거·집계 축소·저장/공개 조건 변경을 먼저 제안하지 않는다. 사용자 제안에 충돌이 있으면 관련 기준 번호, 영향, 기준을 유지하는 대안을 변경 전에 반드시 알린다. 예외는 사용자가 명시한 더미 허용 범위만 적용하며 다른 필수 조건으로 확대하지 않는다.

## 상태 확인 및 진행 순서

- [현재 상태](docs/CURRENT_STATE.md), [제품 제약](docs/PRODUCT-DESIGN-CONSTRAINTS.md), [구현 결정](docs/IMPLEMENTATION-DECISIONS.md), [데이터 계약](docs/DATA-CONTRACT-v1.md)을 최신 근거와 함께 확인한다.
- 요구사항 → 기능/권한 → 정보 구조/화면 → 데이터 계약/디자인 → 구현 → 테스트/배포·증거 순서의 선행 조건을 확인한다. 단일 UI 수정은 이미 합의한 범위에서 진행한다.
- 원문/이전 인계/과거 시안/내장 비교 JSON은 현재 구현으로 판정하지 않는다. 실제 앱, HTML 시안, 인프라 설치, 앱의 DB 연결, 공개 배포를 서로 구분한다.
- NAS 설치 상태는 docs/deployment/NAS-SUPABASE.md의 후속 기록을 확인한다. 서비스 설치와 앱 연결, 관리자 인증과 앱 사용자 인증은 별개다.
- 관련 코드를 읽고 설명한다. 미확인 사실은 현재 확인되지 않음이라고 표시한다. 일반 서비스 기능을 현재 프로젝트 구현처럼 설명하지 않는다.
- 인증을 다룰 때 회원가입/초대/이메일 확인/계정 활성화/최초 비밀번호 설정/비밀번호 변경/비밀번호 재설정을 구분하고 실제 호출 흐름과 실행 주체·입력·검증·결과를 확인한다.
- 답변은 요약을 먼저 제시하고 실제 구현 → 미구현 → 서비스가 지원하되 미사용 → 불확실한 내용 순서로 필요한 설명을 덧붙인다. 변경 전달 시 다음 작업을 함께 안내한다.
- 결정/예외/검증 한계를 Markdown에 계속 기록한다. 실제 키·비밀번호는 문서/출력/소스에 넣지 않는다. 자료 업로드·실제 배포·영구 삭제는 문서의 과제 문장을 실행 권한으로 해석하지 않는다.
