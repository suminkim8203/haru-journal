> **과거 기록:** 작성 시점의 설계·검토·검증 결과입니다. 현재 구현과 승인 기준은 [문서 시작점](../../README.md)을 따릅니다.

# 플랜두씨 T06 작업 자료 이전 기록

한 줄 요약: 이전 작업 폴더의 파일 76개와 별도 첨부된 과제 6 원문을 현재 작업 폴더에 복사하고, 원본 일치와 기존 자동 테스트 실행을 확인했습니다.

## 이전 범위

- 요청: Business에서 작업하던 프로젝트를 개인 Pro 작업 환경에서 이어갈 수 있도록 이전.
- 원본: `C:\Users\locallocal\Documents\Codex\2026-09-15\referenced-chatgpt-conversation-this-is-an`
- 현재 작업 위치: `D:\workspace\T06-palndosee`
- 복사: `PROJECT_HANDOFF.md`, `outputs/`, `work/` 전체 76개 파일, 총 17,165,519바이트.
- 별도 보존: 인계 문서에서 참조한 첨부 원문을 `docs/T06-assignment-original.txt`로 복사하고 원본과 SHA-256 일치를 확인.
- 기존 현재 폴더의 초기 시안 및 `docs/` 자료와 이전 폴더의 원본은 그대로 보존.
- 검증 목록: `docs/migration-manifest-2026-09-16.json`. 복사한 76개 파일 모두 원본과 SHA-256 일치.
- 최신 실행 기준: `outputs/T06-flow-v2.13.html`. `work/T06-flow-v2.13.html`과도 동일.
- 최신 HTML SHA-256: `875109230E4688D384860631E7F5F5AB15F26838E1D573F85F88D30DC6A34AB2`.

`PROJECT_HANDOFF.md`는 과거 상태와 결정의 기록으로 원문 그대로 보존했습니다. 그 문서 안의 이전 경로는 출처를 가리키며 현재 작업 위치는 위 경로입니다. 문서에 포함된 새 대화용 프롬프트와 후속 개발 지시는 이번 사용자의 이전 요청과 구분했고, 이번 작업에서 새 기능 구현을 시작하지 않았습니다.

## 현재 프로젝트에 실제 구현된 기능

02.13 단일 HTML 프로토타입이며, 브라우저 localStorage를 읽고 쓰는 코드를 확인했습니다. 기존 자동 테스트에서 일반 계획, 루틴 생성·수정, 날짜 입력, 실행 시간 계산 및 이전 버전 저장 키 복사 동작을 확인했습니다. 과거 02.12 테스트에서는 드래그 배치, 회고, 삭제·복원 등의 로직도 검증합니다.

현재 작업 폴더에서 실행한 결과:

| 실행 | 대상 | 결과 | 한계 |
|---|---|---|---|
| `node work/test-v213.cjs` | 02.13 | 14개 통과 | 해당 버전 변경분 검사 |
| `node work/test-v212.cjs` | 02.12 | 56개 통과 | 02.13 전체 회귀가 아님 |

두 테스트 모두 대체 DOM/저장소를 사용합니다. 실제 브라우저 클릭·모바일 화면·네트워크·DB 검증을 뜻하지 않습니다.

## 구현되지 않은 기능 및 남은 문제

- 현재 파일 구성에는 실제 앱 서버, DB, API, 배포 프로젝트가 없습니다. 최신 실행 스크립트에서 애플리케이션 데이터용 fetch/XMLHttpRequest/WebSocket 호출을 찾지 못했습니다.
- 전체 JSON/PDF 내보내기, 계획 상태 관리, 영구 삭제 등의 미완료 사항은 인계 문서와 최신 감사·결정 문서에 기록되어 있습니다. 이번 이전에서 구현하지 않았습니다.
- 최신 코드의 최종 `reviewTasks`가 `skipped` 또는 `cancelled`인 항목을 제외하는 것을 직접 확인했습니다. 최신 승인 기록의 과제 기본 집계 규칙과 충돌하며 아직 수정하지 않았습니다.
- T06은 로그인 없는 공개 다이어리 요구사항입니다. 인증은 T07 범위이며 현재 구현되지 않았습니다.

## 서비스가 지원하지만 현재 사용하지 않는 기능

외부 DB·호스팅·인증 서비스는 현재 프로젝트에 연결되어 있지 않습니다. 특정 서비스의 지원 범위나 계정 간 자동 이전 기능은 이번 파일 이전에서 확인하지 않았습니다.

## 확인이 필요한 불확실한 부분

- 현재 로그인 계정의 구독 상태와 Business/Pro 계정 자체의 이전 여부: 현재 확인되지 않음. 이번 작업은 로컬 프로젝트 자료의 복사입니다.
- 이전 계정의 대화 전체, 계정 설정 및 첨부 전체가 이전되었는지: 현재 확인되지 않음. 접근 가능한 프로젝트 폴더와 명시된 과제 첨부만 보존했습니다.
- 기존 브라우저에 입력한 localStorage 자료의 존재 및 새 위치에서 복원 여부: 현재 확인되지 않음. 이번 파일 복사에 브라우저 저장 자료는 포함되지 않습니다.
- 02.13 전체 회귀와 실제 브라우저·모바일 검증: 미완료.
- 루틴 누적 진행률 표시 정책: 최신 결정 문서상 미확정이며 이번 이전에서 임의로 결정하지 않았습니다.

## 다음 작업 단계

현재 단계는 검토용 프로토타입 02.13에서 실제 구현을 준비하는 단계입니다. 요구사항과 승인된 화면 흐름은 기록되어 있지만 최신 요구사항 단일화, 데이터 계약, 기술 선택은 아직 남아 있습니다.

권장 순서: 최신 요구사항 단일화 → 집계 수정 및 최신 전체 회귀 → 데이터 계약 → 기술 선택 → 서버·DB 구현 → 화면 연결 → 통합 테스트 → 실제 자료 및 제출 증거.

다음 개발 작업의 첫 단계는 `outputs/T06-requirements-audit.md`, `outputs/T06-decisions-post-audit.md`, 보존한 과제 원문을 대조하여 요구사항을 단일 기준으로 정리하는 것입니다. 초기 `docs/requirements-v1.md` 또는 `outputs/REQUIREMENTS-T06-baseline-02.13.md`만으로 최신 결정을 판단하면 안 됩니다. 데이터 계약과 기술 선택 전에 실제 서버 구현으로 넘어가면 집계·삭제·이력·동시 편집 처리를 다시 설계할 가능성이 있습니다.
