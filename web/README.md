# React application

Status: development scaffold only. The approved product reference remains ../outputs/T06-flow-v2.24.html.

Run in this directory: npm ci, npm run dev. Checks: npm run typecheck, npm test, npm run build.

Next.js 16.3.5, React 19.3.0, TypeScript. Supabase/PostgreSQL and Vercel are approved but not connected. Environment variable names are placeholders only; no credentials are present.

Do not embed the prototype HTML as the finished application. Port its approved behavior into React components and validate the database contract before production writes. Do not serve local prototype files through this app to bypass browser restrictions.
